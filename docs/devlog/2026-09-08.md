# 2026-09-08 · TinyLSM · Devlog

## Goal 4 · Snapshot, public Iterator and MVCC

状态：`功能完成；性能实验延期`

- Public `Snapshot`/`Iterator` API、per-key multi-version MemTable/SSTable
  ordering和Snapshot watermark compaction retention 已接入；Snapshot 不跨进程恢复。
- `Options::max_active_snapshots` 默认限制 1,024 个活跃 handle；
  `GetSnapshotMetrics()` 暴露 active count、oldest sequence 和最近 full compaction
  的 retained entries/bytes。长期 Snapshot 的近似成本是覆盖写速率乘平均记录大小乘年龄。
- 本机 Debug `./run test`、ASan/UBSan、TSan 均通过 `117/117`，Release build 通过；新增
  固定 seed 参考模型、跨 DB/reopen Snapshot 拒绝、release 后 MVCC 回收、compaction
  Manifest rename failure 与 writer/flush/compact 并发 Snapshot 测试。
  性能压测按风险分层计划延期为独立任务，不阻塞本 Goal 的功能验收；后续只有改动并发或
  所有权时才补对应 sanitizer。

## Goal 5 · SSTable v2 format

状态：`格式核心完成；条件项延期`

- 新写出的 SSTable 使用 v2：data block 内 user key 前缀压缩、可配置 restart interval
  （默认 16）、checksummed restart offsets，以及 checksummed properties section。
- v2 properties 保存 entry count、key range 和 sequence range；Reader 在完整数据校验后将其
  与实际 block 结果交叉检查。启动仍完整验证 Manifest 引用表，因此不以该元数据宣称 Open
  性能提升。
- Reader 与 Lab storage inspector 均先识别固定 36-byte v1 footer，再识别 52-byte v2
  footer；v1 表可继续读，Flush/Compaction 会渐进式重写为 v2。
- 缓存关闭时，v2 point Get 会二分 restart key 并只重建目标 restart group；缓存开启时仍
  使用完整的 validated decoded block。定向验证覆盖 codec/properties/v1 compatibility、
  cache on/off 和 v1/v2 mixed Compaction；性能比较留给后续单独安排的工作。
- Bloom Filter 保持未实现：没有单独的冷读/negative-lookup profile，就不以“经典 LSM
  组件”名义强行加入。这个条件项和 v2 的正式空间/CPU 对比一起延期。

## Goal 6 · bounded group commit

状态：`功能完成；性能实验延期`

- `Put`、`Delete` 和 `WriteBatch` 统一进入按请求数/字节限制的 writer queue；leader 选取
  有界前缀，为完整请求分配连续 sequence，写入一个 WAL batch，并在 `sync_on_write` 下只
  Sync 一次，再整体应用 MemTable。
- 每个公开 `WriteBatch` 不会被拆分。WAL append/sync 失败时，同组调用者收到相同 Status，
  保持原有“失败写需以 reopen 判断最终持久状态”的合同；Close 停止接收新请求并等待已接纳
  队列排空。
- `WriteMetrics` 新增 group、grouped request、writer queue wait/depth 原始计数。一个 4
  writer、每个 8 次写入的定向场景确认发生物理合并、值不丢失，Close/reopen 后 32 条记录
  都可恢复；暂停 WAL Sync 的 Close 场景还确认已接纳 group 会先安全收尾。此阶段最终本机
  Debug 回归为 `119/119`；新增队列后的并发 writer、Snapshot writer/flush/compact、
  group-commit 与 Close 四项 TSan 场景均通过；覆盖新增所有权边界的 8 项 ASan/UBSan
  场景也通过。吞吐/p99 结论明确延期。

## Goal 3 · Compaction metrics and background partial compaction

状态：`完成`

### 已完成并验证

- 选择简化 size-tiered 策略：达到默认四张 SSTable 时，单 worker 合并最老、连续的
  四张表；`Options::compaction_table_trigger = 0` 可关闭自动调度。
- 新增可复算原始指标：point lookup、range scan、scan table input、logical write
  bytes、flush/compaction physical SSTable bytes、input/output table count、live
  SSTable bytes 和 table/byte compaction debt。报告须从这些原始计数计算放大率。
- background worker 继续优先 flush；flush 发布后只在 debt 存在时调度一个 compaction
  job，不 busy-loop。后台 job 先 durable 地保留 output file number，再在锁外读取
  shared SSTable reader snapshot；flush/compaction 的 Manifest publication 均由同一
  state lock 串行化。
- partial job 仅替换最老表前缀，因此没有未参与的更旧值；输出可安全删除该前缀中的
  tombstone。较新的 MemTable/SSTable 仍以更高 sequence 覆盖输出。
- explicit `Compact()` 保留原有“持独占锁、单次 Manifest commit”的恢复边界；它会
  等待已排队的后台工作后重写当时的完整 table set。

### 验证证据

- 本机 `./run format --check`、Debug、ASan/UBSan、TSan 和 Release 通过；三套完整
  test suite 均为 `107/107`，其中本阶段新增五项 background compaction 测试。
- `BackgroundCompactionTest.DropsOnlySafeOldestPrefixTombstoneAndReportsRawMetrics`
  验证两表 tombstone merge 后旧值不会复活，且 table/debt/byte counters 合理。
- `BackgroundCompactionRecoveryTest.FailedCommitKeepsTheOldestPrefixRecoverable`
  在 output Manifest rename 前注入失败：worker 进入 sticky error，reopen 从旧表集
  读回完整数据。
- `BackgroundCompactionConcurrencyTest.ReaderCanOverlapTheCompactionInputSnapshot`
  用文件读取 gate 证明 foreground Get 与后台 compaction input 同时到达 I/O gate，
  再验证 compact 后 Scan 仍完整。
- `BackgroundCompactionTest.LongMixedWorkloadMatchesReferenceAcrossReopen` 使用
  5,000 次 Put/Delete/Get/Scan 与 `std::map` reference 对照，并在 reopen 后复验。
- 固定 Linux VM 两轮均完成 Debug、ASan/UBSan、Clang TSan 的 `107/107`，Release
  build 和 compaction JSON verifier。两份 raw JSON、environment capture 和 benchmark
  log 已存入 `docs/performance/results/`；复现脚本为
  `scripts/run_linux_compaction_goal3.sh`。
- 20,000-operation、五次重复 mixed workload 的 size-tiered median 为 19,266 / 19,640
  ops/s，Manual baseline 为 10,358 / 10,475 ops/s；read amp 从约 16.4 降至约 2.14，
  代价是 write amp 从约 1.286 升至约 7.120。所有 retained throughput CV 均低于 10%。
- 最终测量边界、两轮 latency/space/table/debt 原始数据和“不是已测 leveled 对比”的
  结论已写入 `docs/performance/compaction-2026-09-08.md`。

## Performance extension roadmap closeout

状态：`已验证`

- 路线图已更新为 Goal 1/2/3 均完成，并补齐 Goal 2/3 完成记录、条件项延期理由和
  后续不自动创建 Goal 4 的边界。
- 新增 `docs/performance/performance-extension-closeout-2026-09-08.md`，只索引各 goal
  的 Linux 证据和适用边界，不将不同 workload 的 throughput、RSS 或 p99 合成为总分。
- 保留一项明确未取证：当前 compaction revision 没有独立的干净 checkout Linux Release
  CLI deployment smoke。它不推翻已有 benchmark/sanitizer/recovery 结论；若需部署证明，
  应作为独立小验收运行并保存日志。

## Fixed-Linux Release CLI smoke

状态：`已验证`

- 从本地 Git bundle clone 的干净 `746741e` checkout（`git status --short` 为空）在固定
  Ubuntu ARM64/ext4 VM 完成 Release build。
- 独立 CLI 进程完成 `put deployment linux-ok -> reopen/get -> scan`，并精确断言文本值、
  tab-separated Scan 和 Base64 JSON 输出。环境、build、CLI 与 verification log 已保存到
  `docs/performance/results/goal3-release-smoke-linux-2026-09-08-*`。
- 这补齐路线图的当前 revision Linux deployment smoke。Lab UI submodule 在该 checkout
  未初始化且不参与 CLI 验收；若发布 Lab，须单独构建和验证它。
