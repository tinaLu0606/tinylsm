# 2026-08-21 · TinyLSM · Devlog

> TinyLSM 仍处于学习与评估阶段。本日志中的环境配置已完成，但不代表项目已经正式立项或开始实现。

## macOS C++ 开发环境

状态：`已验证`

### 完成

- 确认 Xcode Command Line Tools 已安装：Apple Clang 21.0.0、Apple Git 2.50.1。
- 用户安装 Homebrew 6.0.18；在 `/Users/tina/.zprofile` 加入 `eval "$(/opt/homebrew/bin/brew shellenv zsh)"`。
- 安装计划中的工具：

```bash
brew install cmake ninja llvm googletest google-benchmark spdlog fmt
```

| 工具 | 版本 |
|---|---:|
| CMake | 4.4.2 |
| Ninja | 1.13.2 |
| Homebrew LLVM/Clang | 22.1.8 |
| GoogleTest | 1.18.0 |
| Google Benchmark | 1.9.5 |
| spdlog | 1.17.0 |
| fmt | 12.2.0 |

### 验证

- 全新登录 zsh 可直接找到 Homebrew、CMake 和 Ninja；`brew doctor` 返回 `Your system is ready to brew.`。
- 临时 CMake/Ninja 工程分别使用 Apple Clang 21 和 Homebrew Clang 22 构建，GTest 均为 `1/1` 通过。
- fmt、spdlog、Google Benchmark 均成功编译、链接并运行。
- Apple Clang 的 AddressSanitizer + UndefinedBehaviorSanitizer 测试通过；临时验证目录已删除。

### 说明与待做

- 默认继续使用 Apple Clang；Homebrew LLVM 为 `keg-only`，需要时显式调用 `/opt/homebrew/opt/llvm/bin/clang++`，未全局设置 LLVM 的 PATH、`LDFLAGS` 或 `CPPFLAGS`。
- Google Benchmark 在当前 macOS 上提示无法读取 CPU 频率和设置线程亲和性；正式性能数据仍需重复测量并在 Linux 复验。
- `未做`：Docker、Linux 虚拟机验证、GitHub Actions、TinyLSM V0 工程初始化。

## 本地 Git 仓库

状态：`已验证`

- 在项目根目录初始化 Git 仓库，默认分支为 `main`。
- 新增 `.gitignore`，忽略 macOS 文件、CMake/Ninja 构建产物、编译/分析产物和本地缓存。
- 现有计划与 devlog 已加入暂存区；尚未创建首个 commit。
- `待确认`：全局 Git 提交身份尚未配置，需要确认姓名和邮箱后再创建首提；未创建 GitHub 远程仓库。
