# 2026-09-05 · TinyLSM · Devlog

## TinyLSM Lab Goal 1

状态：`已验证`

### 完成

- 在 `tools/lab_web` 子模块完成 React/TypeScript 实验台与 typed `LabApi`；当前由确定性 `MockLabApi` 驱动，未接入 C++ 后端。
- 提供 Playground、Storage、Timeline、Workload、Recovery 和 Reports 六个工作区；Mock/Unavailable 数据来源明确标识。
- 增加 IndexedDB 实验保留、报告导入导出、有界指标、窗口化事件日志和 WAL 记录分页。
- 设计文档标记 Goal 1 完成；根 README 与前端 README 说明当前边界和启动方式。

### 验证

- `cd tools/lab_web && npm ci`
- `npm test -- --run`：`7/7` tests passed。
- `npm run build`：通过。
- Vite 浏览器冒烟：桌面与 390 px 移动宽度遍历六个页面，主要交互可用，console 无 error。

### 提交

- 子模块：`cc2b36a feat: build TinyLSM Lab frontend prototype`
- 主仓库：`02a0443 feat: add TinyLSM Lab frontend plan`

### 下一步

- Goal 2：实现 C++ Lab Server、诊断快照和 `HttpLabApi`，以真实 TinyLSM 状态替换 Mock 数据。

## TinyLSM Lab Goal 2 · 后端与传输检查点

状态：`已验证`

### 已完成

- 增加非公开 `DBLabPeer` / `DiagnosticSnapshot`，以拷贝方式提供 MemTable、Manifest、WAL、sequence 和 pending-cleanup 状态；未扩张 `include/tinylsm/db.h` 的公开 API。
- 增加串行 `LabSession`、`tinylsm_lab_server` 和 HTTP 合同：Open/Close/Reopen、Put/Get/Delete/Scan/Compact、state、真实文件列表、operation/event ring buffer、metrics 占位与 SSE `Last-Event-ID` 回放。
- 固定 `cpp-httplib` `v0.18.0` 和 `nlohmann/json` `v3.11.3`；服务器仅接受 loopback 监听，并限制 JSON request body 为 8 MiB。
- 前端新增 `HttpLabApi`；Live 请求统一使用 Base64，且 Live 页面不会复用 Mock 数据。Goal 3/4 的 workload、资源指标、深度解码和 recovery 明确显示为 unavailable。

### 已验证

- `cmake --preset dev-debug && cmake --build --preset dev-debug`：通过。
- `ctest --preset dev-debug --output-on-failure`：`80/80` tests passed，含 Lab Session 与 HTTP/SSE integration tests。
- `cd tools/lab_web && npm test -- --run`：`9/9` tests passed。
- `cd tools/lab_web && npm run build` 与 `VITE_LAB_API=live npm run build`：通过。
- `./run format --check`：通过。
- `./run test`：`80/80` tests passed。
- 浏览器 Live 流程：Open → Put → Get → Scan → Close → Open → Scan；重开后仍读到 `alpha → first value`，页面显示 `LIVE DATA`，console 无 error。

### 提交

- 子模块：`9d1e784 feat: connect Lab UI to live HTTP server`。
- 子模块：`643eb9c fix: label live loading state truthfully`。
